It uses run-length encoding, which is where a numeric value is used to represented repetetions in the file. If the file contains the sequence "WWWW" this can be represented as "4W" after compression, resulting in reduced file size, and easy reverseability.
